import {parseArguments}from'../src/index';import {formatOutput}from'../src/utils/outputFormatter';describe('CLI and Orchestration',()=>{describe('parseArguments',()=>{test('should parse file paths',()=>{const args=['file1.json','file2.json'];const{paths,options}=parseArguments(args);expect(paths).toEqual(['file1.json','file2.json']);expect(options.minify).toBe(true);expect(options.toJson).toBe(true);});test('should parse --no-minify flag',()=>{const args=['file.json','--no-minify'];const{options}=parseArguments(args);expect(options.minify).toBe(false);});test('should parse --no-to-json flag',()=>{const args=['file.json','--no-to-json'];const{options}=parseArguments(args);expect(options.toJson).toBe(false);});test('should parse --cache flag',()=>{const args=['file.json','--cache'];const{options}=parseArguments(args);expect(options.cache).toBe(true);});test('should parse --overwrite flag',()=>{const args=['file.json','--cache','--overwrite'];const{options}=parseArguments(args);expect(options.overwrite).toBe(true);});test('should parse --no-output flag',()=>{const args=['file.json','--no-output'];const{options}=parseArguments(args);expect(options.noOutput).toBe(true);});test('should handle multiple flags',()=>{const args=['file.json','--cache','--overwrite'];const{paths,options}=parseArguments(args);expect(paths).toEqual(['file.json']);expect(options.minify).toBe(true);expect(options.cache).toBe(true);expect(options.overwrite).toBe(true);});test('should handle mixed paths and flags',()=>{const args=['file1.json','--no-minify','file2.json','--cache'];const{paths,options}=parseArguments(args);expect(paths).toEqual(['file1.json','file2.json']);expect(options.minify).toBe(false);expect(options.cache).toBe(true);});test('should set minify to true by default',()=>{const args=['file.json'];const{options}=parseArguments(args);expect(options.minify).toBe(true);});test('should set other options to false by default',()=>{const args=['file.json'];const{options}=parseArguments(args);expect(options.toJson).toBe(true);expect(options.cache).toBe(false);expect(options.overwrite).toBe(false);expect(options.noOutput).toBe(false);});});describe('formatOutput',()=>{test('should format single file as raw content without cache',()=>{const files=[{file:'file.json',content:{a:1},cached:false}];const output=formatOutput(files,{minify:true,toJson:true,cache:false,overwrite:false,noOutput:false});const parsed=JSON.parse(output);expect(parsed).toEqual({a:1});});test('should format single file with cache as wrapped JSON',()=>{const files=[{file:'file.json',content:{a:1},cached:true,cachedPath:'file.compact.json'}];const output=formatOutput(files,{minify:true,toJson:true,cache:true,overwrite:false,noOutput:false});const parsed=JSON.parse(output);expect(parsed).toHaveProperty('content');expect(parsed).toHaveProperty('cached',true);expect(parsed).toHaveProperty('cachedPath');});test('should format multiple files as NDJSON',()=>{const files=[{file:'file1.json',content:{a:1},cached:false},{file:'file2.json',content:{b:2},cached:false}];const output=formatOutput(files,{minify:true,toJson:true,cache:false,overwrite:false,noOutput:false});const lines=output.trim().split('\n');expect(lines).toHaveLength(2);expect(JSON.parse(lines[0])).toEqual({a:1});expect(JSON.parse(lines[1])).toEqual({b:2});});test('should format --no-output as manifest',()=>{const files=[{file:'file1.json',cached:true,cachedPath:'/path/file1.compact.json'},{file:'file2.json',cached:false}];const output=formatOutput(files,{minify:true,toJson:true,cache:true,overwrite:false,noOutput:true});const manifest=JSON.parse(output);expect(manifest).toHaveProperty('processed');expect(manifest).toHaveProperty('total');expect(manifest.processed).toHaveLength(2);expect(manifest.total).toBe(2);});test('should include error field in single file output',()=>{const files=[{file:'file1.json',error:'File not found',cached:false}];const output=formatOutput(files,{minify:true,toJson:true,cache:false,overwrite:false,noOutput:false});const parsed=JSON.parse(output);expect(parsed).toHaveProperty('error');expect(parsed).toHaveProperty('file');});test('should include cached info in output with cache enabled',()=>{const files=[{file:'file1.json',content:{},cached:true,cachedPath:'/path/file1.compact.json'}];const output=formatOutput(files,{minify:true,toJson:true,cache:true,overwrite:false,noOutput:false});const parsed=JSON.parse(output);expect(parsed).toHaveProperty('cached',true);expect(parsed).toHaveProperty('cachedPath','/path/file1.compact.json');});test('should handle three files as NDJSON',()=>{const files=[{file:'file1.json',content:{a:1},cached:false},{file:'file2.json',content:{b:2},cached:false},{file:'file3.json',content:{c:3},cached:false}];const output=formatOutput(files,{minify:true,toJson:true,cache:false,overwrite:false,noOutput:false});const lines=output.trim().split('\n');expect(lines).toHaveLength(3);});});});