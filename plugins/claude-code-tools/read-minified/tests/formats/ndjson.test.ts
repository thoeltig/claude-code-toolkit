import {parseNdjson,formatNdjson,isValidNdjson}from'../../src/formats/ndjson';describe('NDJSON Format Handler',()=>{describe('isValidNdjson',()=>{test('should validate non-empty NDJSON content',()=>{expect(isValidNdjson('{"key":"value"}\n{"key":"value2"}')).toBe(true);});test('should reject empty content',()=>{expect(isValidNdjson('')).toBe(false);});test('should reject whitespace-only content',()=>{expect(isValidNdjson('   \n  \n  ')).toBe(false);});});describe('parseNdjson - Basic Objects',()=>{test('should parse single NDJSON line',()=>{const ndjson='{"name":"John","age":30}';const result=parseNdjson(ndjson);expect(result).toHaveLength(1);expect(result[0]).toEqual({name:'John',age:30});});test('should parse multiple NDJSON lines',()=>{const ndjson='{"id":1,"name":"John"}\n{"id":2,"name":"Jane"}\n{"id":3,"name":"Bob"}';const result=parseNdjson(ndjson);expect(result).toHaveLength(3);expect(result[0].id).toBe(1);expect(result[1].name).toBe('Jane');expect(result[2].id).toBe(3);});test('should handle objects with various data types',()=>{const ndjson='{"string":"value","number":42,"boolean":true,"null":null}\n{"array":[1,2,3],"object":{"nested":"value"}}';const result=parseNdjson(ndjson);expect(result[0].string).toBe('value');expect(result[0].number).toBe(42);expect(result[1].array).toEqual([1,2,3]);});});describe('parseNdjson - Arrays',()=>{test('should handle NDJSON with arrays on each line',()=>{const ndjson='[1,2,3]\n[4,5,6]\n[7,8,9]';const result=parseNdjson(ndjson);expect(result).toHaveLength(3);expect(result[0]).toEqual([1,2,3]);expect(result[2]).toEqual([7,8,9]);});test('should handle mixed objects and arrays',()=>{const ndjson='{"data":123}\n[1,2,3]\n{"name":"test"}';const result=parseNdjson(ndjson);expect(result).toHaveLength(3);expect(typeof result[0]).toBe('object');expect(Array.isArray(result[1])).toBe(true);expect(result[2].name).toBe('test');});});describe('parseNdjson - Empty Lines',()=>{test('should skip empty lines',()=>{const ndjson='{"id":1}\n\n{"id":2}\n\n\n{"id":3}';const result=parseNdjson(ndjson);expect(result).toHaveLength(3);expect(result[0].id).toBe(1);expect(result[2].id).toBe(3);});test('should handle whitespace-only lines',()=>{const ndjson='{"id":1}\n   \n{"id":2}';const result=parseNdjson(ndjson);expect(result).toHaveLength(2);});});describe('parseNdjson - Invalid JSON Handling',()=>{test('should create error objects for invalid JSON lines',()=>{const ndjson='{"valid":"json"}\n{invalid json}\n{"valid":"again"}';const result=parseNdjson(ndjson);expect(result).toHaveLength(3);expect(result[0].valid).toBe('json');expect(result[1].error).toBe('Invalid JSON in line');expect(result[1].raw).toBe('{invalid json}');expect(result[2].valid).toBe('again');});test('should handle malformed JSON with trailing comma',()=>{const ndjson='{"id":1,"name":"test",}\n{"id":2}';const result=parseNdjson(ndjson);expect(result).toHaveLength(2);expect(result[0].error).toBe('Invalid JSON in line');expect(result[1].id).toBe(2);});test('should handle incomplete JSON objects',()=>{const ndjson='{"incomplete":\n{"complete":"object"}';const result=parseNdjson(ndjson);expect(result).toHaveLength(2);expect(result[0].error).toBe('Invalid JSON in line');expect(result[1].complete).toBe('object');});test('should handle JSON with syntax errors',()=>{const ndjson='{"missing":"quote}\n{"valid":true}';const result=parseNdjson(ndjson);expect(result).toHaveLength(2);expect(result[0].error).toBeDefined();expect(result[1].valid).toBe(true);});});describe('parseNdjson - Complex Data',()=>{test('should parse deeply nested objects',()=>{const ndjson='{"level1":{"level2":{"level3":{"value":"deep"}}}}\n{"another":{"nested":"structure"}}';const result=parseNdjson(ndjson);expect(result[0].level1.level2.level3.value).toBe('deep');expect(result[1].another.nested).toBe('structure');});test('should handle special characters in strings',()=>{const ndjson='{"email":"user@example.com","path":"/usr/local/bin","quote":"He said \\"hello\\""}';const result=parseNdjson(ndjson);expect(result[0].email).toBe('user@example.com');expect(result[0].path).toBe('/usr/local/bin');expect(result[0].quote).toBe('He said "hello"');});test('should preserve numeric precision',()=>{const ndjson='{"integer":42,"float":3.14,"scientific":1e5,"negative":-100}';const result=parseNdjson(ndjson);expect(result[0].integer).toBe(42);expect(result[0].float).toBe(3.14);expect(result[0].scientific).toBe(100000);expect(result[0].negative).toBe(-100);});});describe('formatNdjson',()=>{test('should format NDJSON to minified JSON array',()=>{const ndjson='{"id":1}\n{"id":2}';const output=formatNdjson(ndjson,{minify:true});const parsed=JSON.parse(output);expect(parsed).toHaveLength(2);expect(parsed[0]).toEqual({id:1});});test('should format NDJSON to pretty JSON array when not minified',()=>{const ndjson='{"id":1}\n{"id":2}';const output=formatNdjson(ndjson,{minify:false});expect(output).toContain('\n');expect(output).toContain('  ');});test('should handle empty NDJSON',()=>{const output=formatNdjson('',{minify:true});expect(output).toBe('[]');});test('should handle single line NDJSON',()=>{const ndjson='{"single":"line"}';const output=formatNdjson(ndjson,{minify:true});const parsed=JSON.parse(output);expect(parsed).toHaveLength(1);expect(parsed[0].single).toBe('line');});});describe('Integration: Real-world NDJSON Examples',()=>{test('should parse application logs',()=>{const ndjson='{"timestamp":"2024-01-15T10:30:00Z","level":"info","message":"Server started","pid":1234}\n{"timestamp":"2024-01-15T10:30:01Z","level":"debug","message":"Connection established","pid":1234}\n{"timestamp":"2024-01-15T10:30:02Z","level":"error","message":"Connection failed","pid":1234,"error":"Timeout"}';const result=parseNdjson(ndjson);expect(result).toHaveLength(3);expect(result[0].level).toBe('info');expect(result[1].message).toBe('Connection established');expect(result[2].error).toBe('Timeout');});test('should parse database records export',()=>{const ndjson='{"id":1,"email":"john@example.com","created_at":"2024-01-01","active":true}\n{"id":2,"email":"jane@example.com","created_at":"2024-01-02","active":true}\n{"id":3,"email":"bob@example.com","created_at":"2024-01-03","active":false}';const result=parseNdjson(ndjson);expect(result).toHaveLength(3);expect(result[0].email).toBe('john@example.com');expect(result[2].active).toBe(false);});test('should parse streaming API responses',()=>{const ndjson='{"status":"processing","progress":0}\n{"status":"processing","progress":50}\n{"status":"complete","progress":100,"result":"success"}';const result=parseNdjson(ndjson);expect(result).toHaveLength(3);expect(result[0].progress).toBe(0);expect(result[2].result).toBe('success');});});});
