import {formatPlaintext,handlePlaintextContent}from'../../src/formats/plaintext';describe('Plaintext Format Handler',()=>{describe('formatPlaintext',()=>{test('should return content unchanged when minify=false',()=>{const input='line 1\n\nline 2';const result=formatPlaintext(input,{minify:false});expect(result).toBe(input);});test('should return content when minify=true',()=>{const input='line 1\nline 2';const result=formatPlaintext(input,{minify:true});expect(result).toBe(input);});});describe('handlePlaintextContent',()=>{test('should return string content as-is',()=>{const content='Some text content';const result=handlePlaintextContent(content);expect(result).toBe(content);expect(typeof result).toBe('string');});test('should handle multiline content',()=>{const content='Line 1\nLine 2\nLine 3';const result=handlePlaintextContent(content);expect(result).toBe(content);});test('should handle empty string',()=>{const content='';const result=handlePlaintextContent(content);expect(result).toBe('');});});});