import {parseMarkdown,formatMarkdown,isValidMarkdown}from'../../src/formats/markdown';describe('Markdown Format Handler',()=>{describe('isValidMarkdown',()=>{test('should validate non-empty markdown content',()=>{expect(isValidMarkdown('# Title\nSome content')).toBe(true);});test('should reject empty content',()=>{expect(isValidMarkdown('')).toBe(false);});test('should reject whitespace-only content',()=>{expect(isValidMarkdown('   \n  \n  ')).toBe(false);});});describe('parseMarkdown - Headings',()=>{test('should parse heading level 1',()=>{const md='# Main Title';const result=parseMarkdown(md);expect(result).toHaveLength(1);expect(result[0].type).toBe('heading');expect(result[0].level).toBe(1);expect(result[0].content).toBe('Main Title');});test('should parse all heading levels',()=>{const md='# H1\n## H2\n### H3\n#### H4\n##### H5\n###### H6';const result=parseMarkdown(md);expect(result).toHaveLength(6);expect(result[0].level).toBe(1);expect(result[1].level).toBe(2);expect(result[5].level).toBe(6);});test('should handle heading with markdown elements',()=>{const md='# See [GitHub](https://github.com)';const result=parseMarkdown(md);expect(result[0].content).toBe('See [GitHub](https://github.com)');});});describe('parseMarkdown - Paragraphs',()=>{test('should parse simple paragraph',()=>{const md='This is a paragraph.';const result=parseMarkdown(md);expect(result).toHaveLength(1);expect(result[0].type).toBe('paragraph');expect(result[0].content).toBe('This is a paragraph.');});test('should parse multiple paragraphs separated by blank lines',()=>{const md='First paragraph.\n\nSecond paragraph.';const result=parseMarkdown(md);expect(result).toHaveLength(2);expect(result[0].content).toBe('First paragraph.');expect(result[1].content).toBe('Second paragraph.');});test('should treat multiple consecutive lines as single paragraph',()=>{const md='Line 1\nLine 2\nLine 3';const result=parseMarkdown(md);expect(result).toHaveLength(1);expect(result[0].content).toContain('Line 1');expect(result[0].content).toContain('Line 2');});});describe('parseMarkdown - Formatting Stripping',()=>{test('should strip bold markdown',()=>{const md='This is **bold** text.';const result=parseMarkdown(md);expect(result[0].content).toBe('This is bold text.');});test('should strip italic markdown',()=>{const md='This is *italic* text.';const result=parseMarkdown(md);expect(result[0].content).toBe('This is italic text.');});test('should strip strikethrough markdown',()=>{const md='This is ~~strikethrough~~ text.';const result=parseMarkdown(md);expect(result[0].content).toBe('This is strikethrough text.');});test('should strip multiple formatting types',()=>{const md='**Bold**, *italic*, and ~~strike~~ text.';const result=parseMarkdown(md);expect(result[0].content).toBe('Bold, italic, and strike text.');});});describe('parseMarkdown - Preserved Inline Elements',()=>{test('should preserve markdown links',()=>{const md='Visit [GitHub](https://github.com) for code.';const result=parseMarkdown(md);expect(result[0].content).toContain('[GitHub](https://github.com)');});test('should preserve inline code',()=>{const md='Use `const x = 1;` in your code.';const result=parseMarkdown(md);expect(result[0].content).toContain('`const x = 1;`');});test('should preserve multiple links',()=>{const md='Check [docs](https://docs.com) and [api](https://api.com).';const result=parseMarkdown(md);expect(result[0].content).toContain('[docs](https://docs.com)');expect(result[0].content).toContain('[api](https://api.com)');});});describe('parseMarkdown - Code Blocks',()=>{test('should parse code block with language',()=>{const md='```typescript\nconst x = 1;\nreturn x;\n```';const result=parseMarkdown(md);expect(result).toHaveLength(1);expect(result[0].type).toBe('code');expect(result[0].language).toBe('typescript');expect(result[0].content).toContain('const x = 1');});test('should parse code block without language',()=>{const md='```\nplain code\n```';const result=parseMarkdown(md);expect(result[0].language).toBe('');});test('should preserve code formatting and newlines',()=>{const md='```js\nfunction test() {\n  return true;\n}\n```';const result=parseMarkdown(md);expect(result[0].content).toContain('function test()');expect(result[0].content).toContain('return true');});});describe('parseMarkdown - Lists',()=>{test('should parse unordered list',()=>{const md='- item 1\n- item 2\n- item 3';const result=parseMarkdown(md);expect(result).toHaveLength(1);expect(result[0].type).toBe('list');expect(result[0].ordered).toBe(false);expect(result[0].items).toHaveLength(3);expect(result[0].items[0]).toBe('item 1');});test('should parse ordered list',()=>{const md='1. first\n2. second\n3. third';const result=parseMarkdown(md);expect(result[0].type).toBe('list');expect(result[0].ordered).toBe(true);expect(result[0].items).toHaveLength(3);});test('should handle list items with formatting',()=>{const md='- **important** item\n- item with [link](url)';const result=parseMarkdown(md);expect(result[0].items[0]).toBe('important item');expect(result[0].items[1]).toContain('[link](url)');});test('should skip nested list items and parse top-level items',()=>{const md='- item 1\n  - nested 1\n  - nested 2\n- item 2';const result=parseMarkdown(md);expect(result[0].items.length).toBeGreaterThan(0);expect(result[0].items).toContain('item 1');expect(result[0].items).toContain('item 2');});});describe('parseMarkdown - Task Lists',()=>{test('should parse task list with checked items',()=>{const md='- [x] completed task\n- [ ] incomplete task';const result=parseMarkdown(md);expect(result).toHaveLength(1);expect(result[0].type).toBe('list');expect(result[0].items[0].checked).toBe(true);expect(result[0].items[0].content).toBe('completed task');});test('should parse mixed task and regular list items',()=>{const md='- [x] task\n- regular item\n- [ ] another task';const result=parseMarkdown(md);expect(result[0].items).toHaveLength(3);});});describe('parseMarkdown - Blockquotes',()=>{test('should parse simple blockquote',()=>{const md='> This is a quote';const result=parseMarkdown(md);expect(result).toHaveLength(1);expect(result[0].type).toBe('blockquote');expect(result[0].content).toContain('This is a quote');});test('should parse multiline blockquote',()=>{const md='> Line 1\n> Line 2\n> Line 3';const result=parseMarkdown(md);expect(result[0].content).toContain('Line 1');expect(result[0].content).toContain('Line 2');});test('should preserve markdown in blockquote',()=>{const md='> See [link](url) for details';const result=parseMarkdown(md);expect(result[0].content).toContain('[link](url)');});});describe('parseMarkdown - Tables',()=>{test('should parse markdown table',()=>{const md='| Header 1 | Header 2 |\n|----------|----------|\n| Cell 1   | Cell 2   |\n| Cell 3   | Cell 4   |';const result=parseMarkdown(md);expect(result).toHaveLength(1);expect(result[0].type).toBe('table');expect(result[0].headers).toEqual(['Header 1','Header 2']);expect(result[0].rows).toHaveLength(2);});test('should parse table with alignment markers',()=>{const md='| Left | Center | Right |\n|:-----|:-----:|------:|\n| a    | b     | c     |';const result=parseMarkdown(md);expect(result[0].headers).toHaveLength(3);expect(result[0].rows[0]['Left']).toBe('a');});test('should handle table cells with markdown',()=>{const md='| col |\n|-----|\n| **bold** text |\n| [link](url) |';const result=parseMarkdown(md);expect(result[0].rows[0].col).toBe('bold text');expect(result[0].rows[1].col).toContain('[link](url)');});});describe('parseMarkdown - Horizontal Rules',()=>{test('should parse horizontal rule with dashes',()=>{const md='Text before\n\n---\n\nText after';const result=parseMarkdown(md);expect(result.some(block=>block.type==='hr')).toBe(true);});test('should parse horizontal rule with asterisks',()=>{const md='---\n\nContent';const result=parseMarkdown(md);expect(result[0].type).toBe('hr');});});describe('parseMarkdown - Mixed Content',()=>{test('should parse document with multiple block types',()=>{const md='# Title\n\nParagraph text.\n\n- item 1\n- item 2\n\n```js\ncode\n```';const result=parseMarkdown(md);expect(result.some(b=>b.type==='heading')).toBe(true);expect(result.some(b=>b.type==='paragraph')).toBe(true);expect(result.some(b=>b.type==='list')).toBe(true);expect(result.some(b=>b.type==='code')).toBe(true);});test('should parse nested structures (lists in blockquotes)',()=>{const md='> - quoted item 1\n> - quoted item 2';const result=parseMarkdown(md);expect(result[0].type).toBe('blockquote');});});describe('parseMarkdown - Edge Cases',()=>{test('should handle heading followed by paragraph',()=>{const md='# Title\nThis is content under the heading';const result=parseMarkdown(md);expect(result).toHaveLength(2);expect(result[0].type).toBe('heading');expect(result[1].type).toBe('paragraph');});test('should handle empty lines between blocks',()=>{const md='First\n\n\n\nSecond';const result=parseMarkdown(md);expect(result.length).toBeLessThan(5);});test('should handle code-like content in paragraphs',()=>{const md='Use `code` and [links](url) together';const result=parseMarkdown(md);expect(result[0].content).toContain('`code`');expect(result[0].content).toContain('[links](url)');});test('should not parse links in code blocks',()=>{const md='```\n[not a link](url)\n```';const result=parseMarkdown(md);expect(result[0].type).toBe('code');expect(result[0].content).toContain('[not a link](url)');});});describe('parseMarkdown - Front Matter',()=>{test('should preserve YAML front matter',()=>{const md='---\ntitle: Document\nauthor: Test\n---\n\n# Content';const result=parseMarkdown(md);expect(result[0].type).toBe('frontmatter');expect(result[0].content).toContain('title: Document');});});describe('formatMarkdown',()=>{test('should format markdown to minified JSON',()=>{const md='# Title\n\nContent.';const output=formatMarkdown(md,{minify:true});const parsed=JSON.parse(output);expect(parsed).toBeInstanceOf(Array);expect(parsed.length).toBeGreaterThan(0);});test('should format markdown to pretty JSON when not minified',()=>{const md='# Title';const output=formatMarkdown(md,{minify:false});expect(output).toContain('\n');expect(output).toContain('  ');});});describe('Integration: Real-world Markdown Examples',()=>{test('should parse README.md style document',()=>{const md='# Project Name\n\n## Description\nThis is a project.\n\n## Installation\n```bash\nnpm install\n```\n\n## Usage\n- Clone repo\n- Install deps\n- Run build';const result=parseMarkdown(md);expect(result.some(b=>b.type==='heading'&&b.level===1)).toBe(true);expect(result.some(b=>b.type==='code'&&b.language==='bash')).toBe(true);expect(result.some(b=>b.type==='list')).toBe(true);});test('should parse API documentation',()=>{const md='## GET /users/:id\n\n> Retrieve user by ID\n\n| Param | Type |\n|-------|------|\n| id | string |';const result=parseMarkdown(md);expect(result.some(b=>b.type==='heading')).toBe(true);expect(result.some(b=>b.type==='blockquote')).toBe(true);expect(result.some(b=>b.type==='table')).toBe(true);});test('should parse document with mixed inline formatting',()=>{const md='Visit **[our website](https://example.com)** or read the *`API docs`*.';const result=parseMarkdown(md);expect(result[0].content).toContain('[our website](https://example.com)');expect(result[0].content).toContain('`API docs`');});});});
